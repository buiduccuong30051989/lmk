// Quick test to verify JSX files are working
import { allPages } from './masterData.jsx'

console.log('✅ JSX files loaded successfully!')
console.log(`📊 Total pages loaded: ${allPages.length}`)

export default function quickTest() {
  return true
}
